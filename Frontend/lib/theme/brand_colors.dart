import 'package:flutter/material.dart';

/// Paleta derivada del logo Alerta Vital
class BrandColors {
  // Morado del texto “ALERTA VITAL”
  static const primary = Color(0xFF8A5CF6); // morado vibrante

  // Rojo/coral de manos y corazón
  static const secondary = Color(0xFFFF5A5F); // coral-rojizo

  // Rojo de alerta (triángulo / SOS)
  static const alert = Color(0xFFFF3B30);

  // Superficies suaves
  static const surface = Color(0xFFF7F8FA);
  static const surfaceAlt = Color(0xFFF0F2F5);

  // Texto
  static const textPrimary = Color(0xFF1E1E1E);
  static const textSecondary = Color(0xFF5F6368);
}
